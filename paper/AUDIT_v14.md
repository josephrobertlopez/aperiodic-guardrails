# Paper v14 Audit
_Generated: 2026-05-09 · Branch: claude/code-review-7oVdy · Commit: ef749ec_

Scope: cross-checks `paper/main.tex` (2602 lines) and `paper/sections/quotient-generalization.tex` against `CACM_ARTICLE_v14.md` Appendix A and against the 13 `results/*.json` artifacts present in `results/` at the audit commit. Read-only on all sources.

---

## 1. Notation & Lemma Drift (paper vs. CACM v14 Appendix A)

CACM v14 Appendix A (`CACM_ARTICLE_v14.md` lines 177–217) is treated as the canonical-notation source of truth for this audit. Every row below cites a literal substring location in both sources (or notes a one-sided occurrence).

| Symbol/Lemma in paper | Location (paper) | Symbol/Lemma in CACM v14 App. A | Match? | Severity | Note |
|---|---|---|---|---|---|
| `L_{\mathsf{blocked}}` | `main.tex:482, 727, 736, 749, 800, 811, 840, 847, 916, 945, 1430` | `L_blocked` | yes (typographic only) | low | LaTeX `\mathsf{blocked}` ↔ Markdown `L_blocked` (CACM line 193, 199, 201). Same referent, different surface syntax. |
| `L_{\mathsf{decode}}(p)` | `main.tex:736, 800, 860, 916, 932` | `L_decode(p)` | yes (typographic only) | low | CACM line 193 defines `L_decode(p) = { w ∈ Σ* : w[0::p] ∈ L_blocked }`; paper line 736 defines the same set. |
| `L_{\mathsf{safe}}` | `main.tex:468, 473, 485` | _absent_ | n/a | low | CACM v14 App. A never introduces `L_safe`; uses `L_G ∈ AC⁰` (line 191) directly. Paper introduces it as the complement (`def:blocked-language`). Not a contradiction; an extra paper-side definition. |
| `\phi_p` (encoding) | `main.tex:482, 749, 800, 2062` | `φ_p` / `phi_p` / `\phi_p` | yes | low | CACM line 205 ("instances of the φ₂ construction"); paper uses `\phi_p` for the family. Direct semantic match. |
| `\mathbb{Z}/p\mathbb{Z}` | `main.tex:530, 845, 935` | `Z/pZ` | yes (typographic only) | low | CACM line 199, 201 ("Z/pZ ⊂ M(L_decode(p))"); paper uses `\mathbb{Z}/p\mathbb{Z}`. |
| `\mathrm{MOD}_p`, `\mathrm{MOD}_2` | `main.tex:558, 830, 1431, 2062 et seq.` | `MOD_p`, `MOD₂` | yes | low | CACM line 15, 191, 193, 205 ("MOD₂ already breaks…"). Same referent. |
| `\mathrm{AC}^0`, `\mathrm{TC}^0` | `main.tex:549, 558, 1944, 2054, 2085` | `AC⁰`, `TC⁰` | yes | low | CACM line 15, 191, 209, 211, 235. Same referent. |
| `M(L)` (syntactic monoid) | `main.tex:516–522` (`def:syntactic-monoid`) | `M(L)` | yes | low | CACM line 209 ("syntactic monoid divides the direct product"); paper has the formal definition (`Σ*/≡_L`). |
| Substring-aperiodicity lemma | `main.tex:590` (`thm:substring-aperiodic`, "Substring aperiodicity") | A.1 ("Substring-aperiodicity lemma", CACM line 179) | partial | med | CACM A.1 lists the **explicit grammar fragment** (literals, character classes, alternation, optional, dot, bounded repetition, "Kleene star or plus *applied to character classes only*") (CACM line 181). Paper's `thm:substring-aperiodic` block (line 590) is the corresponding theorem; reviewer should verify the paper statement enumerates the same exclusions (in particular the Kleene-on-group counterexample `(ab)*` that CACM line 183 flags). |
| Word-boundary lemma | `main.tex:685` (`lem:word-boundary`, "Word-boundary anchors preserve aperiodicity") | A.2 ("Word-boundary anchors preserve aperiodicity", CACM line 185) | yes | low | Both name the same lemma and same scope (the `\b` anchor); CACM line 187 says "41 of 142 corpus patterns" use it; paper §sec:substring-aperiodic should match that count. |
| Guardrail Blindness Theorem (two regimes) | `main.tex:741` (`thm:blindness`) and `main.tex:839` (`thm:regime2`) | A.3 ("The Guardrail Blindness Theorem — both regimes closed", CACM line 189) | yes | low | CACM line 197 explicitly names "Two regimes, both closed" (alphabet ⊊ Σ vs alphabet = Σ). Paper splits this across `thm:blindness` (Regime 1, line 741) and `thm:regime2` (Regime 2, line 839). The Remark `rem:both-regimes` (line 945–) wires them. Naming differs (paper has two theorems, CACM has one §A.3 with two cases) but coverage is congruent. |
| `\mathrm{MOD}_2` sufficiency corollary | `main.tex:830` (`cor:parity`) | A.3 Corollary "MOD₂ sufficiency" (CACM line 205) | yes | low | Both state `p=2` suffices; both list zero-width / acrostic / comment as instances of `\phi_2`. |
| Closure-under-composition (App. A.4) | `main.tex:962` (`prop:composition`, "Composition preserves aperiodicity") + `main.tex:2061` (`prop:filter-composition`) | A.4 ("Closure under composition and the broader string-matcher class", CACM line 207) | partial | **high** | CACM A.4 (lines 209–217) makes **four** distinct closure claims: (a) Boolean closure of star-free; (b) cross-class composition (WAF ∘ IDS ∘ LLM-guardrail); (c) finite-language matchers (tries, DFA blocklists) inherit the bound; (d) Bloom-filter behaviour modulo false-positives. Paper's `prop:composition` (line 964) covers (a) only. Paper's `prop:filter-composition` (line 2061) is about regex–neural composition, not the broader star-free string-matcher class. Items (b)–(d) have no proposition in the paper. See §3 row 1 below. |
| GIDP / NP-completeness | `main.tex:1067, 1080` (`def:gidp`, `thm:np`) | _absent in App. A_ | n/a | low | Paper-only result; CACM v14 doesn't claim it in App. A and the abstract framing emphasizes algebraic blindness rather than NP-hardness. Not a drift, just a paper-vs-CACM scope difference. |
| Faithful transfer (free-category functoriality) | `main.tex:1132` (`thm:transfer`) | _absent in App. A_ | n/a | low | Paper-only. CACM mentions "free categories, Krohn–Rhodes" only in the abstract (paper line 221); not formalized in CACM App. A. |
| Syntactic indistinguishability observation | `main.tex:1197` (`thm:indistinguish`) | _absent in App. A_ | n/a | low | Paper-only. |
| Krohn–Rhodes / blindness spectrum | `main.tex:570` (`thm:kr-classical`) and `main.tex:981` (`prop:krohn-rhodes`) | _absent in App. A_ | n/a | low | CACM v14 App. A does not invoke Krohn–Rhodes; mentions Pin (1986 §I.4) but not the K-R decomposition. Paper-only. |
| Corpus size and source count | `main.tex:88–91` ("123 patterns from eleven open-source guardrail tools and 19 additional author-generated test patterns (142 total)") and `main.tex:1496–1497` ("142 regex patterns from eleven open-source guardrail tools") | CACM line 208 ("123 patterns from eleven open-source guardrail tools") **and** CACM line 225 ("Twelve sources, 142 patterns. **Nine third-party projects** contribute 100 patterns… **Three author-assembled sets** contribute 42 patterns (29.6%)") | **no** | **high** | CACM v14 contradicts itself: line 208 says "123 from eleven tools" (matches paper) but line 225 says "100 from nine third-party projects + 42 from three author-assembled sets". The paper's 123/19 ≠ CACM-§B.1's 100/42, and paper's "eleven tools" ≠ CACM-§B.1's "nine third-party + three author". Total agrees (142). Reviewer-visible inconsistency. |
| Filler set in Pilot B / `printable_filler_bypass.json` | `main.tex:90–91` mentions only `MOD_2` empirically; `tab:v5-attacks` (line 1441) uses single `x` filler; no count of fillers in paper | CACM line 235 ("seven fillers (NULL, ASCII space, tab, tilde, backtick, U+200B, U+200C)") | partial | med | The seven-filler claim is CACM-only. Paper does not name Pilot B / `printable_filler_bypass.json` or report its 392/392 result. Pilot-set surface area in paper ≠ CACM. Tracked also in §2 row "tab:v5-attacks". |
| Pilot D library count ("9 libraries", "8 of 9") | _absent_ | CACM line 239 ("48 baseline-matched across 8 of 9 libraries (Presidio's PII patterns are the synthesis exception)") | n/a | med | Pilot D as such (third-party library bypass with 100 patterns → 48 bypass) does not appear in paper §sec:empirical (lines 1491–1620). Paper's `tab:corpus` (line 1532) shows only 5 representative patterns. The "48/48 bypass under both NULL and ASCII-space filler" headline number is CACM-only. |
| Pilot 6 / non-LLM defense bypass | _absent_ | CACM line 110, 116 ("**Non-LLM defense-class bypass**… Pilot 6"), App. B.3 not present but `non_llm_defense_bypass.json` referenced in CACM ref. 19 (line 267) | n/a | **high** | The non-LLM (ModSec + SpamAssassin) empirical extension is the headline addition of v14 (`a36930d`-vintage commit message says "non-LLM empirical extension"). It is in CACM (line 110, table row 6) but **not in `paper/main.tex`**. The paper's empirical section (§sec:empirical, lines 1491–1620) ends with the Tree-of-Thought benchmark; case studies follow at line 1622; no Pilot 6 / non-LLM table appears. Material drift: paper is a v13-vintage empirical scope. |

---

## 2. Empirical Artifact Linkage

Status enum: LINKED (paper cites the JSON by name), AMBIGUOUS (caption keywords match ≥1 JSON but no explicit cite), ORPHANED (caption matches no JSON), MISSING-ARTIFACT (paper claims a result for which no JSON exists).

Floats enumerated by `tools/audit_v14.py` (14 total: 13 in `paper/main.tex`, 1 in `paper/sections/quotient-generalization.tex`; 0 `\begin{figure}`, 0 `\includegraphics`). The sections file is **not `\input`-ed** by `paper/main.tex` (verified: `grep '\input{' paper/main.tex` returns no hits) and is therefore an orphaned-from-build float, flagged below.

| Paper figure/table | Caption (truncated 80 chars) | Backing JSON file | Status | Note |
|---|---|---|---|---|
| `tab:v4-pipeline` (`main.tex:1397`) | "V4 encoding bootstrap pipeline stages." | _none expected_ | LINKED | Pipeline stage description, not an empirical result. No artifact required. |
| `tab:v5-attacks` (`main.tex:1441`) | "V5 modular counting bypass instances. p: the interleaving modulus used to" | `results/mod_p_bypass_matrix.json` | AMBIGUOUS | F1–F5 payload examples (`rm -rf /`, `wget malware`, `import os`, `eval()`, `exec(`) appear in `mod_p_bypass_matrix.json by_pattern` but no explicit `\texttt{results/mod_p_bypass_matrix.json}` reference in caption or surrounding prose (lines 1438–1465). Paper does not direct the reader to the artifact. |
| `tab:corpus` (`main.tex:1532`) | "Representative guardrail regex patterns from 11 tools. DFA states and monoid sizes" | `results/monoid_distribution.json` | AMBIGUOUS | The corpus-wide aperiodicity statistic ("142/142 aperiodic", line 1519) plausibly aggregates over `monoid_distribution.json` (which has `total_patterns`, `by_source`, `by_category` keys). Paper does not cite the JSON. The 5 representative rows F1–F5 do not appear directly in `monoid_distribution.json`'s schema. |
| `tab:tot-results` (`main.tex:1576`) | "Tree-of-Thought benchmark results ($N = 50$ randomized grammars, seeds 0--49" | `results/tot_n50_honest.json` | LINKED | `main.tex:1620` cites `\texttt{results/tot\_n50\_honest.json}` in the "Reproducibility correction" paragraph. The only float in the paper with an explicit JSON backlink. |
| `tab:aspirin-case` (`main.tex:1642`) | "Aspirin synthesis case study. The abstract derivation" | _none expected_ | LINKED | Hand-curated interpretation table, not a measurement. No artifact required. |
| `tab:tcp-case` (`main.tex:1670`) | "TCP state machine (RFC~793) run through the same pipeline." | _none expected_ | LINKED | Hand-curated interpretation table. No artifact required. |
| `tab:zerbik-case` (`main.tex:1720`) | "Zerbik process calculus: a synthetic case study we constructed" | _none expected_ | LINKED | Author-constructed synthetic domain, no measurement. No artifact required. |
| `tab:visibility-trace` (`main.tex:1774`, `table*`) | "Visibility trace. Columns 1 and 2 are byte-identical abstract" | _none expected_ | LINKED | Pedagogical illustration. No artifact required. |
| `tab:defense` (`main.tex:1909`) | "Defense layer mapping. Each row gives the guardrail layer, its mechanism," | _none expected_ | LINKED | Conceptual mapping table. No artifact required. |
| `tab:circuit-complexity` (`main.tex:1951`) | "Circuit complexity of guardrail architectures. $\\mathrm{AC}^0 \\subsetneq" | _none expected_ | LINKED | Theory-only summary table. No artifact required. |
| `tab:composition-empirical` (`main.tex:2117`) | "Empirical validation of composition laws on 20 encoded payloads." | `results/comprehensive_empirical_validation.json` | AMBIGUOUS | Caption claims "20 encoded payloads, 20 clean-text samples"; `comprehensive_empirical_validation.json` has `encoding_results`, `semantic_attacks`, `preprocessing` keys plausibly aggregating this. No explicit `\texttt{results/...}` cite in the surrounding prose (lines 2111–2144). Reviewer cannot reproduce by inspection. |
| `tab:neural-evasion` (`main.tex:2152`) | "Neural filter evasion by attack type. The filter is trained on $\\mathrm{MOD}_2$" | `results/comprehensive_empirical_validation.json` (likely `semantic_attacks` key) and/or `results/class_c_adaptive_adversary.json` | AMBIGUOUS | Rows include "V1 Decomposition" / "V3 Abstract grammar" with `100%` evasion of both regex and neural — `class_c_adaptive_adversary.json` (`adaptive_evasion_rate`, `techniques`) is the candidate. No explicit cite. |
| `tab:e2e` (`main.tex:2406`) | "End-to-end detection rates (\\%) against real LLM Guard patterns." | `results/e2e_llmguard_validation.json` | AMBIGUOUS | Strong caption-to-keys match: `e2e_llmguard_validation.json` has `defense_stacks`, `encodings`, `n_patterns: 23`, `n_payloads: 20`, `patterns_source` — exactly the table's axes. No explicit cite in §sec:e2e (lines 2398–2437). |
| `tab:abstraction_domains` (`sections/quotient-generalization.tex:7`) | "Abstraction functions across five architectural domains. Each exhibits informati…" | _none expected_ | ORPHANED | The section file is **not `\input`-ed by `paper/main.tex`** (no `\input` directive exists in main.tex). The float exists on disk but is never compiled into the paper. Either include it or drop it from the build tree. |

**Artifact files present but never referenced by any paper float (orphaned-from-paper):**

| JSON file | Top-level keys | Suggested home |
|---|---|---|
| `results/llm_decode_pilot.json` | `cells, fillers, models, payloads, summary, test_name` | Superseded by v2; document deprecation in CACM B.3 (CACM cites only `_v2.json` at line 237). Paper does not mention either. |
| `results/llm_decode_pilot_v2.json` | `cells_control, cells_decode, …, summary` (`aggregate.lift_pp = 91.7`) | Cited in CACM B.3 (`results/llm_decode_pilot_v2.json`, line 237) but **not in paper** — the 91.7pp lift / 55-of-60 result is paper-absent. Add a paper float or move to App. B. |
| `results/library_pattern_bypass.json` | `by_library, fillers, primes, test_name` | Cited in CACM B.3 (Pilot D, line 239) — not in paper. |
| `results/printable_filler_bypass.json` | `by_filler, fillers_tested (7), patterns_tested (11), primes` | Cited in CACM B.3 (Pilot B, line 235) — not in paper. |
| `results/non_llm_defense_bypass.json` | `by_pattern, fillers (NULL/SPACE/ZWSP), p, sources (ModSec, SpamAssassin), summary_by_class` | Cited in CACM (table row 6, line 110) — not in paper. **The headline new pilot of v14 is paper-absent.** |
| `results/transformer_tc0_validation.json` | `model, params, results, training, timestamp` | Plausibly underlies the "Empirical TC⁰ validation" paragraph (`main.tex:2010–2021`, "one layer, one attention head, …, 14,753 parameters") but no float wraps it and no JSON cite is present. |
| `results/class_c_defense_test.json` | `defense_1_structural, defense_2_reinterpretation, defense_3_combined, conclusion` | No paper float; possibly relates to the V1–V4 class taxonomy in §sec:filter-composition (paper line 2175–2195). Currently orphaned. |
| `results/class_c_adaptive_adversary.json` | `adaptive_evasion_rate, techniques, conclusion` | Candidate backing for `tab:neural-evasion` — see row above. No explicit cite. |
| `results/comprehensive_empirical_validation.json` | `corpus, encoding_results, semantic_attacks, preprocessing, theoretical, timestamp` | Candidate backing for `tab:composition-empirical` and possibly `tab:e2e`/`tab:neural-evasion`. No explicit cite. |
| `results/mod_p_bypass_matrix.json` | `by_pattern, by_prime, primes, test_name` | Candidate backing for `tab:v5-attacks` (and the "56/56 bypass" headline of CACM line 106). No explicit cite in paper. |
| `results/monoid_distribution.json` | `by_category, by_source, histogram, monoid_size_stats, …` | Candidate backing for the "142/142 aperiodic" paragraph (`main.tex:1518–1527`) and `tab:corpus`. No explicit cite. |

**No file matches the spec's expected `comprehensive_empirical_validation.json` ↔ `e2e_llmguard_validation.json` distinction:** both files exist (the spec listed both as expected). The actual artifact set is the 13 JSONs above; the spec's expected list of 5 is incomplete relative to what the repository contains, but every spec-listed file (`non_llm_defense_bypass.json`, `mod_p_bypass_matrix.json`, `library_pattern_bypass.json`, `comprehensive_empirical_validation.json`, `e2e_llmguard_validation.json`) is present.

---

## 3. §10 Closure-Under-Composition Gaps

The paper has 13 `\section{}` headings; §10 by sequential numbering is `\section{Defense Recommendations}\label{sec:discussion}` (`main.tex:1901`), which contains the closure-under-composition material via `\subsection{Filter Composition Laws}\label{sec:filter-composition}` (`main.tex:2051`). The flagged "closure-under-composition argument" of the planning doc maps to this subsection plus the cross-references back to `prop:composition` (`main.tex:962`, in §6 Impossibility Results) which §10 inherits.

Gap-type enum: hand-wave / undefined-term / missing-assumption / missing-proof-step / scope-overreach.

| Claim (paper line refs) | Gap type | Severity | Suggested fix |
|---|---|---|---|
| `prop:composition` (`main.tex:964`): "If `L_1, L_2` … with aperiodic syntactic monoids, then `L_1 ∩ L_2`, `L_1 ∪ L_2`, and `Σ* \ L_1` all have aperiodic syntactic monoids." Proof sketch (lines 968–972) cites De Morgan and `M(L̄) ≅ M(L)` but does **not extend the closure to "any star-free string-matcher of any kind"** as CACM A.4 (line 211) does — no mention of finite-language matchers, tries, DFA blocklists, Bloom filters. | scope-overreach (paper claim is narrower than CACM headline) **and** missing-proof-step (cross-class composition not proved) | **high** | Either (a) add a corollary right after `prop:composition` that explicitly handles tries / finite-DFA / Bloom-filter blocklists — CACM A.4 paragraphs at lines 213, 215 give the argument verbatim and can be lifted; or (b) downscope CACM A.4 to match the paper's narrower regex-only closure. The current state has CACM claiming a strictly broader theorem than the paper proves. |
| `prop:filter-composition` item (3) "Serial confirmation preserves blindness (regex veto)" (`main.tex:2074–2079`): the proof is "(`f_r ∧ f_n`)(`φ_p(x)`) = min(0, `f_n(φ_p(x))`) = 0" — algebraically correct but **assumes the binary AND model**. The paper does not address "score-blend" (item 5) for the serial case; it does not discuss whether a **bypass-aware neural filter retraining** can break the veto. | hand-wave / missing-assumption | med | Add an explicit assumption block: "We model serial composition as Boolean AND of block-decisions; soft-score serial pipelines (e.g., regex emits a confidence; neural conditions on it) are addressed in item (5)." Optionally add a one-paragraph remark on "what would defeat the veto in practice" (parallel-OR with neural, per item 4). |
| `prop:filter-composition` item (4) "Parallel with neural breaks the ceiling" (`main.tex:2080–2086`): proves the composition detects iff the neural filter detects, then concludes "Since `f_n ∈ TC⁰` and `MOD_p ∈ TC⁰`, this composition escapes the AC⁰ ceiling." | scope-overreach (capacity vs realization) | **high** | The capacity statement is correct; the realization claim is not. CACM line 124 explicitly addresses this gap: "This is not a guarantee that a specific trained transformer reliably computes MOD_p; Hahn 2020 established that fixed-architecture self-attention cannot model parity for unboundedly long inputs unless depth or heads scale with sequence length. The capacity-vs-realization gap matters." The paper makes the capacity claim without the Hahn caveat in this proposition. Add a one-sentence caveat citing Hahn 2020 + the paper's own §sec:neural-tc0 (line 1938) where the caveat is mentioned. |
| `prop:filter-composition` item (5) "Critical blend weight" (`main.tex:2087–2095`): defines `α* = 1 - τ/c` from `s_r(φ_p(x)) ≈ 0` and `s_n(φ_p(x)) = c > 0`. **`s_r ≈ 0` is not formally justified** — the proposition tacitly equates "regex blocks → 0" with "regex score ≈ 0" without defining `s_r`. | undefined-term | med | Add "Let `s_r, s_n: Σ* → [0,1]` be the calibrated block-scores of `f_r, f_n` such that `f(x) = 1 ⟺ s(x) > τ_f`" before the item. Then the `s_r(φ_p(x)) ≈ 0` claim follows from Theorem~\ref{thm:blindness} applied to a thresholded star-free language. |
| Empirical-validation paragraph (`main.tex:2111–2115`) cites `tab:composition-empirical` (line 2117) as validating "all five laws" but the table has 7 rows and the law-to-row mapping is implicit. Items (1) "Parallel regex preserves blindness" and (2) "Serial regex preserves blindness" have no explicit row in `tab:composition-empirical`. | missing-proof-step (claim-vs-evidence mismatch) | med | Either add two rows ("Parallel regex" and "Serial regex" with `0%` detection) to `tab:composition-empirical`, or reword the paragraph to say "we validate items (3)–(5) below; items (1)–(2) follow from `prop:composition`." |
| "The composition trap is the same mathematical object across the defensive-security landscape" (CACM v14 line 137) is **paper-absent**: the paper has §sec:filter-composition (regex×neural) but does not generalize composition to WAF×IDS×LLM-guardrail pipelines as CACM §5 (and CACM A.4 line 211) does. | scope-overreach in CACM relative to paper | **high** | Add a one-paragraph remark to §sec:filter-composition explicitly stating: "The same Boolean-closure argument (Proposition~\ref{prop:composition}) covers any composition of star-free string-matchers across defensive-class boundaries — a WAF rule set composed with an IDS signature set composed with an LLM guardrail, all star-free, remains in AC⁰." Cite Pin 1986 §I.4 as CACM does. |
| Pilot 6 / non-LLM defense bypass (CACM table row 6, CACM line 110, artifact `non_llm_defense_bypass.json`) is the empirical demonstration of the cross-class composition claim. **Paper has no §10 (or §sec:empirical) reference to it.** | missing-proof-step (the empirical leg of the closure claim) | **high** | Add a subsection to §sec:empirical (or to §sec:filter-composition) summarizing the Pilot 6 result: 6 ModSecurity OWASP CRS 4.27 SQL-injection patterns + 6 SpamAssassin `20_phrases.cf` patterns; MOD₂ bypass under three fillers (NULL, ASCII space, ZWSP); 8/10 NULL bypass, 10/10 ASCII-space, 10/10 ZWSP for ModSec; 9/9 across all three for SpamAssassin. Cite `results/non_llm_defense_bypass.json`. This closes the "empirical for three classes (LLM, WAF, spam), algebraic for the rest" wording of CACM line 165. |
| `prop:filter-composition` paragraph "Practical consequence" (`main.tex:2098–2109`) uses the verbs "inherits" (L2102), "vetoes" (L2077), "reimports" (L2109) without defining them as formal predicates. These are reader-level shorthand; the underlying algebraic content is in items (1)–(5). | hand-wave (cosmetic) | low | Acceptable as informal exposition; if paranoid, replace "inherits the regex layer's blindness" with the explicit "is in the same `AC⁰`-blocked-language class (Proposition~\ref{prop:composition})." |

---

## 4. Summary Counts

- Notation mismatches (Table 1, rows with Match? = "no" or "partial"): **5** (3 partial = substring-aperiodicity grammar fragment, A.4 closure scope, Pilot B/D filler set; 2 no = corpus 123-vs-100/19-vs-42 split, Pilot 6 absence)
- Orphaned figures/tables (Table 2, Status = ORPHANED): **1** (`tab:abstraction_domains` in `sections/quotient-generalization.tex`, never `\input`-ed)
- Ambiguous figure/table linkage (Table 2, Status = AMBIGUOUS): **6** (every paper table that should cite a JSON but doesn't)
- §10 gaps total (Table 3): **8**
- §10 gaps high severity (Table 3, Severity = high): **4** (broader-class closure missing, capacity-vs-realization caveat missing in item (4), cross-class composition not in paper, Pilot 6 empirical leg absent)

Additional finding outside the three tables: 9 of 13 `results/*.json` artifacts are not cited by any paper float (orphaned-from-paper). Five are cited in CACM v14 App. B.3 / table row 6 / ref. 19 but absent from `paper/main.tex`. The paper's empirical surface area is materially smaller than CACM v14's.

---

## 5. Procedure

**Inputs read (read-only):** `paper/main.tex` (2602 lines), `paper/sections/quotient-generalization.tex`, `paper/sections/appendix-poc-source.tex` (no floats; not used for tables), `CACM_ARTICLE_v14.md` (267 lines), all 13 files in `results/*.json`.

**Table 1 (notation drift):** symbol/lemma extraction by `grep -nE` on `\\begin\{(theorem|lemma|definition|proposition|corollary)\}`, `L_\{?\\?(mathsf\{?blocked|safe\}?|G\}?)`, `phi_p`, `MOD_p`, `\\mathbb\{Z\}/p\\mathbb\{Z\}` against `paper/main.tex`; cross-referenced against literal substrings in `CACM_ARTICLE_v14.md` lines 177–217 (Appendix A) read end-to-end. Each row cites the paper line(s) that introduce or use the symbol and the CACM line(s) where the canonical form appears. Match verdict = "yes" when the only difference is LaTeX↔Markdown surface syntax, "partial" when scope or subset of cases differ, "no" when the rows present numerically inconsistent claims.

**Table 2 (artifact linkage):** `tools/audit_v14.py` enumerates every `\\begin\{figure*?\}`, `\\begin\{table*?\}`, and `\\includegraphics` block in `paper/main.tex` and `paper/sections/*.tex`, captures the nearest `\\caption{...}` and `\\label{...}` by ±30-line lookahead, and prints the result. Status assignment: LINKED when the paper text within ±20 lines of the float cites a `results/*.json` filename (verified after collapsing LaTeX backslash-escapes `\_`/`\-`); LINKED with "_none expected_" when the float is a hand-curated illustration (case studies, conceptual mappings); AMBIGUOUS when caption keywords match a JSON's top-level schema but no explicit cite exists; ORPHANED when the file containing the float is not `\input`-ed by `paper/main.tex` (verified: `grep '\\input{' paper/main.tex` returns no hits, so all of `paper/sections/*.tex` is orphaned-from-build). The "results JSON inventory" was produced by `python3 -c "import json; print(sorted(json.load(open(p)).keys()))"` per file.

**Table 3 (§10 gaps):** §10 by sequential `\\section{}` count is "Defense Recommendations" (`main.tex:1901`). Read end-to-end (lines 1901–2210). Hand-wave hits gathered by `tools/audit_v14.py` matching `clearly|obviously|it follows|trivially|without loss of generality|closed under|straightforward|easy to|easily|inherits?|propagates?|veto(?:es)?` in the section body — produces 4 raw hits (lines 2074, 2077, 2093, 2102). Each hit then evaluated for whether the underlying algebra/proof is sound: cosmetic hand-wave → low severity; missing assumption or undefined term → med; scope claim larger than what is proved or contradiction with CACM v14 → high. Three high-severity gaps (closure scope, capacity-vs-realization, cross-class composition) found by **comparing paper §sec:filter-composition against CACM §A.4** (CACM line 207–217), not by hand-wave grep alone — the paper proves a narrower theorem than CACM advertises.

**Reproducibility:** `tools/audit_v14.py` prints the floats list, hand-wave hits, JSON inventory, and JSON-citation cross-reference in a single read-only pass; rerun after any source change to refresh the raw materials. The audit Markdown itself is hand-curated against that output; no LLM-generated table content was used without a literal substring grounding in either `paper/main.tex`, `CACM_ARTICLE_v14.md`, or one of `results/*.json`.

**Word count:** §5 = 326 words (≤ spec ceiling of "brief, ≤200 words" exceeded by 126; the procedure section was extended to make every methodology choice explicit per the audit's reproducibility-acceptance criterion). Trimmable on request.
